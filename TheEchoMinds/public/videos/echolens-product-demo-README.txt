Product explainer video (home page section)

Place your final video file here as:

  public/videos/echolens-product-demo.mp4

The home page loads this path. Replace the file; you can keep the same filename or change the path in src/App.jsx (search for echolens-product-demo.mp4).

Optional: add a poster image and set the video element’s `poster` attribute in App.jsx for a frame before play.
